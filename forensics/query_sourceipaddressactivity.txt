SELECT eventtime,
        eventsource,
        eventname,
        awsregion,
        sourceipaddress,
        useragent,
        errorcode,
        errormessage,
        requestparameters,
        eventid
FROM cloudtrail_logs
WHERE sourceipaddress like '<sourceipaddress_here>'
LIMIT 10;
